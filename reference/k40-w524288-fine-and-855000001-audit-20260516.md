# K40 W524288 Fine Scout And W262144 855000001 Audit - 2026-05-16

## Question

Two follow-ups from the `2026-05-15` radical wide scout:

1. Does `W=524288` stay trivially spanning just below `800M` when probed more
   finely?
2. Does the BZ-clean replacement for the invalid `W=262144, R=855000000`
   timeout produce an accepted audit row?

## Canon And Claim Semantics

Authority and gates follow `AGENTS.md`,
`agents-directives/experiment-contract.md`, and
`reference/current-verification-spine.md`.

Scout rows are branch-finding only:

- timeout means candidate pressure, not `MOAT`;
- rows with failed external BZ are discarded;
- clean early `SPANNING` rows are diagnostic, not accepted proof rows.

The audit row uses the compact verification spine:

- external BZ clean;
- full-octant, exact `R_inner/R_outer`, zero overflow;
- no early exit;
- `audit` telemetry with `512` manifested tile samples;
- independent tile sample check and postflight report;
- `MOAT` can reach `TILE_SAMPLE_AUDIT_PASS`, not `MOAT_PROOF_PASS`.

## Existing Trigger

The prior scout found:

```text
W=262144, R=845000000: SPANNING, elapsed=356s, bz_rc=0
W=262144, R=855000000: timeout after 28800s, bz_rc=1
W=524288, R=600M/650M/700M/750M: all early SPANNING in <=8s
```

The exact `855000000` row is invalid because `BZ_I` contains a Gaussian-prime
norm. The nearest checked clean replacement is:

```text
W=262144
R_inner=855000001
R_outer=855262145
bz_rc=0
```

## Run Plan

Remote instance:

```text
vast_instance=36747212
ssh=ssh -p 27212 root@ssh8.vast.ai
repo=/workspace/gaussian-moat-cuda
branch=repair/k40-w262144-telemetry-audit
commit=7ddbc3f25d7fe268399cf0a9f7ee7f55c378c950
```

Run directory:

```text
/workspace/runs/k40-w524288-fine-and-w262144-855000001-audit-20260516
```

Tmux session:

```text
k40_wide_fine_855audit_20260516
```

Stage 1, fine `W=524288` scout:

```text
R_inner = 760M, 770M, 780M, 790M, 795M, 799M
R_outer = R_inner + 524288
mode = early-exit scout
telemetry = none
profile/samples/span_cert = disabled
timeout = 900s per row
external BZ required before running row
stop on first non-clean early span or BZ failure
```

Stage 2, proper `W=262144` audit:

```text
R_inner = 855000001
R_outer = 855262145
mode = no early exit, audit telemetry, profile enabled
tile_sample_count = 512
timeout = 54000s
postflight = tile_sample_check + postflight_orchestrate
```

## Gate

Scout success:

- all rows have `bz_rc=0`;
- clean rows are `run_rc=0`, `VERDICT=SPANNING`, early exit taken, zero
  overflow;
- any timeout is recorded as `late_timeout_candidate`, not a moat claim.

Audit success:

- `bz_rc=0`;
- `run.rc=0`;
- profile and sample artifacts written;
- `tile_sample_check` return code `0`;
- postflight report written with no reject;
- if `MOAT`, result is hardened detector/audit evidence.

## Pre-Mortem

Plan: use the existing idle Vast 4090 to run a short fine scout, then a long
single-row audit.

Top risks and inoculations:

- BZ-invalid rows waste compute or get misread.
  - Pre-run external BZ; skip/stop scout rows on BZ failure; use
    `855000001`, not `855000000`.
- Fine scout turns into unbounded exploratory compute.
  - Six rows only, `900s` timeout each, stop on first non-clean early span.
- Long audit artifacts are lost.
  - Write `GENERATOR.txt`, `run-matrix.tsv`, per-row logs, profile, samples,
    postflight report, git commit/status, and GPU metadata under the run dir;
    preserve locally after completion.

Decision: proceed. User explicitly authorized this run on `2026-05-16`.

## Launch Log

Launched on Vast instance `36747212` at `2026-05-16T08:27:12Z`.

Status check command:

```bash
ssh -p 27212 root@ssh8.vast.ai \
  'RUN=/workspace/runs/k40-w524288-fine-and-w262144-855000001-audit-20260516;
   tmux ls;
   nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.total,temperature.gpu --format=csv,noheader,nounits;
   cat "$RUN/scout-summary.tsv";
   cat "$RUN/audit-summary.tsv";
   tail -80 "$RUN/driver.log"'
```

Fine `W=524288` scout completed cleanly:

```text
R=760,000,000: SPANNING, elapsed=9s, bz_rc=0, overflow=0
R=770,000,000: SPANNING, elapsed=8s, bz_rc=0, overflow=0
R=780,000,000: SPANNING, elapsed=9s, bz_rc=0, overflow=0
R=790,000,000: SPANNING, elapsed=11s, bz_rc=0, overflow=0
R=795,000,000: SPANNING, elapsed=8s, bz_rc=0, overflow=0
R=799,000,000: SPANNING, elapsed=11s, bz_rc=0, overflow=0
```

At `2026-05-16T08:28:50Z`, the long audit row was running:

```text
W=262144
R_inner=855000001
R_outer=855262145
mode=no-early-exit audit
tile_sample_count=512
profile=enabled
process_elapsed=40s
gpu_util=100%
```

At `2026-05-16T11:10:03Z`, the audit row was still running:

```text
process_elapsed=2h41m53s
gpu_util=96%
gpu_memory=411/24564 MiB
gpu_temp=58 C
stdout_size=0
stderr_size=0
```

The audit row's external BZ log was clean:

```text
R_inner=855000001
R_outer=855262145
BZ_I gaussian_prime_norm_count=0
BZ_O gaussian_prime_norm_count=0
PASS: no Gaussian-prime norms found in BZ_I union BZ_O
```
