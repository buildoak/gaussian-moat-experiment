[package]
name = "fat-stripe"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "fat-stripe"
path = "src/main.rs"

[dependencies]
moat-kernel = { path = "../moat-kernel" }
clap = { version = "4", features = ["derive"] }
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
