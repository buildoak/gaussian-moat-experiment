## Task: Independent investigation of 3 bugs flagged in tile-validator Python code

You are an independent auditor. Another engine (Gemini) flagged three bugs in the tile-validator Python code. A third engine (Opus) is investigating the same bugs simultaneously. Your findings will be compared.

### BUG-01: Face boundary 1-off
Claim: `in_tile` check `0 <= row < 256` excludes a 257th point because S=256 lattice units means 257 lattice points.

Investigate by reading:
- `docs/tile_spec.md` -- exact definition of tile side S and lattice point ownership
- `tile-validator/ports.py` -- the in_tile check and face boundary ranges
- `tile-validator/tile.py` -- sieve window vs tile coordinate mapping
- `tile-cpp/include/constants.h` -- C++ tile side definition
- `tile-cpp/src/face_extract.cpp` -- C++ face boundary logic

Key question: does a tile of side S=256 contain 256 points (indices 0..255) or 257 points (indices 0..256)? Check what happens at the shared boundary between adjacent tiles -- is there a double-counting risk?

### BUG-02: I/O vs L/R axis swap
Claim: tile_spec.md defines I/O on the y-axis (radial), but Python assigns I/O to the a-coordinate (x-axis).

User says: I = bottom, O = top. That is the canonical interpretation.

Investigate by reading:
- `docs/tile_spec.md` -- EXACT definition of I, O, L, R faces and which coordinate axis each lives on
- `docs/tile_operations.md` -- same definitions
- `tile-validator/ports.py` -- which coordinate (a or b, row or col) does Python check for each face?
- `tile-cpp/src/face_extract.cpp` -- which coordinate does C++ check for each face?

Key question: in the lattice where a=real (x) and b=imaginary (y), is face I at min-b (bottom) or min-a (left)?

### BUG-03: Simplified overflow bridge
Claim: compose.py handles overflow with a boolean instead of conservative bridge (unioning all ports through poisoned tile).

Investigate by reading:
- `docs/tile_spec.md` -- what does spec say about composing overflow tiles?
- `docs/compositor_spec.md` -- full compositor spec for overflow handling
- `tile-validator/compose.py` -- actual overflow handling code

Key question: does this bug affect per-tile validation, or only grid-level composition?

### Output
Three verdicts:
- CONFIRMED / FALSE POSITIVE / PARTIALLY CONFIRMED
- Evidence with file:line references
- Impact: per-tile, composition, or both?
- Fix if confirmed

Be precise. Under 500 words.
